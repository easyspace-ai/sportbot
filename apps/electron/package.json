{
  "name": "@polyzeng/electron",
  "version": "0.1.0",
  "description": "Electron shell: loads apps/dashboard in dev and packaged builds",
  "main": "dist/main.cjs",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build:main": "bun build src/main/index.ts --outfile=dist/main.cjs --target=node --format=cjs --external=electron",
    "build:preload": "bun build src/preload/preload.ts --outfile=dist/preload.cjs --target=node --format=cjs --external=electron",
    "build:copy": "bun ../../scripts/electron-build-resources.ts",
    "build": "bun run build:main && bun run build:preload && bun run build:copy",
    "dev": "vite dev --config vite.config.mjs --host 127.0.0.1 --strict-port --port 5173",
    "typecheck": "tsc --noEmit",
    "start": "bun run build && electron ."
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^5.1.2",
    "vite": "^6.2.4"
  }
}
