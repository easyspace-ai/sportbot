# Bot HTTP + WS relay — must match `PORT` in apps/bot/src/embeddedEnv.ts (default 7633).
# Use 127.0.0.1 (not localhost): bot binds HOST=127.0.0.1 only; localhost→::1 breaks Vite proxy.
VITE_DEV_API_ORIGIN=http://127.0.0.1:7633
